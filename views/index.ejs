<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="/js/script.js" defer></script>
  </head>
  <body>
    <div class="container">
      <div class="heading">
        <h3 class="title"><%=title%></h3>
        <div class="buttons">

          <button class="btn" data-id="add">Add User</button>
          <button class="btn" data-id="sort">Sort</button>
        </div>
      </div>
      <ul class="content" id="content">
        <% users.forEach((user) => { %>
          <%-include("components/item", { user })%>
        <% }) %>
      </ul>
      <%-include("components/modal")%>
    </div>
    <div class="notification__container" id="notificationContainer"></div>
  </body>
</html>
